# بورتفوليو يحيى أحمد

موقع شخصي لعرض الأعمال — مبني بـ React + Vite + Tailwind CSS مع مشاهد ثلاثية الأبعاد.

## التشغيل محلياً
```bash
npm install
npm run dev
```

## البناء للنشر
```bash
npm run build
```

الناتج يظهر في مجلد `dist`.

---
المستودع: `hhyehia33-hue/yehia-portfolio`
