# نشر الموقع — دليل بسيط خطوة بخطوة

## قبل كل حاجة: خطوة واحدة مهمة

الموقع فيه صورتك، بس اللي في الملفات دلوقتي صورة مؤقتة.
عشان صورتك الأصلية تظهر لكل الزوار:

1. خد صورتك اللي بعتها قبل كده على جهازك.
2. غيّر اسمها لـ: `yehia-portrait.png`
3. انسخها بدل الملف الموجود في مجلد: `public/images/`

---

## الطريقة الأسهل (دقيقتين — بدون أي برامج)

ده أسهل طريق. النتيجة النهائية: لينك مباشر لموقعك شغال على النت.

### الخطوة 1 — افتح موقع Vercel:
افتح اللينك ده في المتصفح:
**https://vercel.com/signup**

سجّل حساب جديد. أسهل حاجة تضغط **"Continue with GitHub"** وتسجل بنفس إيميل حساب GitHub بتاعك.

### الخطوة 2 — انشر الموقع:
بعد ما تسجل دخول:

1. افتح الصفحة دي: **https://vercel.com/new**
2. هتلاقي كارت مكتوب عليه **"Deploy"** تحته كلمة صغيرة **"without Git"** — اضغط عليها.
3. هيظهرلك مربع كبير بيقولك اسحب ملفاتك هنا.
4. روح لمجلد المشروع عندك، واسحب المجلد اللي اسمه **`dist`** وأفلته جوه المربع ده.
5. هيشتغل كام ثانية… وهيطلعلك اللينك بتاعك، شكله كده:
   **https:// اسم-موقعك .vercel.app**

خلاص، الموقع شغال! افتح اللينك وجربه، وجرب فورم الواتساب كمان.

---

## لو عايز الموقع على GitHub كمان (اختياري لكن مفيد)

GitHub زي نسخة احتياطية دائمة لكود موقعك. وميزته: أي تعديل تعمله بعد كده ينزل على الموقع لوحده بدون ما تعيد النشر.

### أولاً — اعمل مستودع جديد:

1. افتح: **https://github.com/new**
2. في خانة "Repository name" اكتب: `yehia-portfolio`
3. سيب الاختيار **Public** زي ما هو.
4. اضغط الزر الأخضر **"Create repository"**

### ثانياً — ارفع ملفات المشروع:

1. في الصفحة اللي هتفتح، هتلاقي سطر فيه لينك أزرق مكتوب عليه:
   **"uploading an existing file"** — اضغط عليه.
2. هيبديلك مربع تسحب فيه الملفات. اسحب الملفات والمجلدات دي من مجلد المشروع وأفلتها:
   - مجلد `src`
   - مجلد `public`
   - ملف `index.html`
   - ملف `package.json`
   - ملف `package-lock.json`
   - ملف `vite.config.ts`
   - ملف `tsconfig.json`
   - ملف `tsconfig.app.json`
   - ملف `vercel.json`
   - ملف `.gitignore`

   **(متسحبش مجلد `node_modules` ولا مجلد `dist` — مش محتاجينهم هنا)**

3. انزل تحت واضغط الزر الأخضر **"Commit changes"**.

### ثالثاً — اربط Vercel بـ GitHub:

1. افتح: **https://vercel.com/new**
2. اضغط زرار **"Import Git Repository"**
3. هتلاقي اسم المستودع `yehia-portfolio` ظاهر — اضغط جنبه **"Import"**
4. متغيّرش أي حاجة في الإعدادات، بس اضغط الزر الأزرق **"Deploy"**
5. استنى دقيقة… وهيديك اللينك المباشر.

**من هنا ورايح:** أي تعديل تعمله في الملفات على GitHub، الموقع بيتحدث تلقائياً بعده بدقيقة.

---

## بخصوص قاعدة بيانات Neon

باختصار: **مش محتاجينها دلوقتي.**

فورم التواصل في الموقع بيبعت رسالة العميل على واتسابك مباشرة، ومفيش بيانات لازم تتخزن في قاعدة بيانات.

لو يوم حبيت تحفظ رسائل العملاء في جدول جوه الموقع نفسه (بدل الواتساب أو جنبه)، ساعتها نستخدم Neon — وكلمتها وهنضيفها عادي.
